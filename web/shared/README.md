# Web Shared

Shared web code that is not owned by one product feature.

## Contents

- `api`: fetch client helpers.
- `auth`: token and user persistence.
- `types`: API DTOs used across features.
- `utils`: formatting helpers.

Keep feature-specific state and UI inside `web/features/*`.
