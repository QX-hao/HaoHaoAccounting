# Web Components

Shared layout components used across feature pages.

- `AuthGate`: validates browser token before showing protected pages.
- `AppShell`: sidebar navigation and app layout.
- `PageFrame`: common authenticated page wrapper.

Feature-specific components should live under `web/features/<feature>/components`.
