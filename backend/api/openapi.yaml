openapi: 3.0.3
info:
  title: HaoHaoAccounting API
  version: 0.1.0
  description: |
    Cross-origin browser requests must come from an origin configured in CORS_ALLOW_ORIGINS.
    Requests from other origins are rejected by CORS middleware with 403 before route handlers run
    and do not include Access-Control-Allow-Origin.
  contact:
    name: HaoHaoAccounting Maintainers
    url: https://github.com/QX-hao/HaoHaoAccounting/security
servers:
  - url: http://localhost:8080/api/v1
    description: Local development API server.
externalDocs:
  description: Repository API contract and verification workflow.
  url: https://github.com/QX-hao/HaoHaoAccounting/tree/dev-pxhao#协作与安全
tags:
  - name: auth
    description: Authentication and current user endpoints.
  - name: accounts
    description: Account management endpoints.
  - name: budgets
    description: Budget management endpoints.
  - name: categories
    description: Category management endpoints.
  - name: transactions
    description: Transaction management endpoints.
  - name: ai
    description: AI assisted parsing endpoints.
  - name: reports
    description: Reporting and analytics endpoints.
  - name: dataio
    description: Import and export endpoints.
security:
  - bearerAuth: []
paths:
  /auth/login:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    post:
      tags: [auth]
      operationId: postAuthLogin
      summary: Log in with username and password
      description: Verifies the configured admin credentials and returns a bearer token plus the current user profile.
      security: []
      requestBody:
        description: Username and password credentials for starting an authenticated session.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/LoginRequest'
      responses:
        '200':
          description: Login success
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
              example:
                token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example.signature
                user:
                  id: 1
                  name: 好好用户
                  username: admin
                  phone: ''
                  email: ''
                  wechatId: ''
        '401':
          $ref: '#/components/responses/Unauthorized'
        '429':
          $ref: '#/components/responses/RateLimited'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
  /auth/refresh:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    post:
      tags: [auth]
      operationId: postAuthRefresh
      summary: Refresh the current access token
      description: Issues a fresh token for the authenticated user without changing the user profile.
      responses:
        '200':
          description: Refreshed token
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/LoginResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
  /auth/logout:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    post:
      tags: [auth]
      operationId: postAuthLogout
      summary: Log out the current user
      description: Revokes the presented bearer token so it cannot be used for later authenticated requests.
      responses:
        '200':
          description: Logout success
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
            Clear-Site-Data:
              $ref: '#/components/headers/ClearSiteData'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OkResponse'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
  /me:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    get:
      tags: [auth]
      operationId: getMe
      summary: Get the current user profile
      description: Returns the profile associated with the authenticated bearer token.
      responses:
        '200':
          description: Current user
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/CurrentUser'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
  /accounts:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    get:
      tags: [accounts]
      operationId: getAccounts
      summary: List accounts
      description: Returns all accounts available to the authenticated user.
      responses:
        '200':
          description: Account list
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Account'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
    post:
      tags: [accounts]
      operationId: postAccounts
      summary: Create an account
      description: Creates a new account with its opening balance and metadata.
      requestBody:
        description: Account details to create.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/AccountRequest'
      responses:
        '201':
          description: Created account
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Location:
              $ref: '#/components/headers/Location'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
  /accounts/{id}:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    put:
      tags: [accounts]
      operationId: putAccountsById
      summary: Update an account
      description: Replaces an existing account with the supplied account details.
      parameters:
        - $ref: '#/components/parameters/Id'
      requestBody:
        description: Complete account details to replace the existing account.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/AccountRequest'
      responses:
        '200':
          description: Updated account
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Account'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
    delete:
      tags: [accounts]
      operationId: deleteAccountsById
      summary: Delete an account
      description: Deletes the selected account when it is not blocked by related ledger data.
      parameters:
        - $ref: '#/components/parameters/Id'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
  /budgets:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    get:
      tags: [budgets]
      operationId: getBudgets
      summary: List budgets
      description: Returns budgets, optionally filtered to a specific YYYY-MM month.
      parameters:
        - name: month
          in: query
          description: Budget month in YYYY-MM format.
          schema:
            type: string
            pattern: '^\d{4}-\d{2}$'
          example: '2026-06'
      responses:
        '200':
          description: Budget list
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Budget'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
    post:
      tags: [budgets]
      operationId: postBudgets
      summary: Create a budget
      description: Creates a monthly budget for a category.
      requestBody:
        description: Monthly category budget details to create.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/BudgetRequest'
      responses:
        '201':
          description: Created budget
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Location:
              $ref: '#/components/headers/Location'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Budget'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
  /budgets/{id}:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    put:
      tags: [budgets]
      operationId: putBudgetsById
      summary: Update a budget
      description: Replaces an existing monthly budget with the supplied budget details.
      parameters:
        - $ref: '#/components/parameters/Id'
      requestBody:
        description: Complete monthly budget details to replace the existing budget.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/BudgetRequest'
      responses:
        '200':
          description: Updated budget
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Budget'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
    delete:
      tags: [budgets]
      operationId: deleteBudgetsById
      summary: Delete a budget
      description: Deletes the selected monthly budget.
      parameters:
        - $ref: '#/components/parameters/Id'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
  /categories:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    get:
      tags: [categories]
      operationId: getCategories
      summary: List categories
      description: Returns categories, optionally filtered by transaction type.
      parameters:
        - name: type
          in: query
          description: Transaction type filter.
          schema:
            $ref: '#/components/schemas/TransactionType'
          example: expense
      responses:
        '200':
          description: Category list
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
    post:
      tags: [categories]
      operationId: postCategories
      summary: Create a category
      description: Creates a category for classifying income or expense transactions.
      requestBody:
        description: Category details to create.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/CategoryRequest'
      responses:
        '201':
          description: Created category
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Location:
              $ref: '#/components/headers/Location'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
  /categories/{id}:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    put:
      tags: [categories]
      operationId: putCategoriesById
      summary: Update a category
      description: Replaces an existing category with the supplied category details.
      parameters:
        - $ref: '#/components/parameters/Id'
      requestBody:
        description: Complete category details to replace the existing category.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/CategoryRequest'
      responses:
        '200':
          description: Updated category
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Category'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
    delete:
      tags: [categories]
      operationId: deleteCategoriesById
      summary: Delete a category
      description: Deletes the selected category when it is not blocked by related ledger data.
      parameters:
        - $ref: '#/components/parameters/Id'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '400':
          $ref: '#/components/responses/BadRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '403':
          $ref: '#/components/responses/Forbidden'
        '404':
          $ref: '#/components/responses/NotFound'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
  /transactions:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    get:
      tags: [transactions]
      operationId: getTransactions
      summary: List transactions
      description: Returns paginated transactions filtered by date range, type, category, account, or keyword.
      parameters:
        - name: page
          in: query
          description: Page number. Defaults to 1 when omitted.
          schema:
            type: integer
            minimum: 1
            default: 1
          example: 1
        - name: pageSize
          in: query
          description: Number of items per page. Defaults to 20 when omitted.
          schema:
            type: integer
            minimum: 1
            maximum: 200
            default: 20
          example: 20
        - name: start
          in: query
          description: Inclusive start datetime. Accepts RFC3339, YYYY-MM-DD, YYYY-MM-DD HH:mm:ss, or YYYY/MM/DD.
          schema:
            type: string
            format: date-time
          example: '2026-06-01T00:00:00+08:00'
        - name: end
          in: query
          description: Inclusive end datetime. Date-only values cover the entire day. Accepts RFC3339, YYYY-MM-DD, YYYY-MM-DD HH:mm:ss, or YYYY/MM/DD.
          schema:
            type: string
            format: date-time
          example: '2026-06-30'
        - name: type
          in: query
          description: Transaction type filter.
          schema:
            $ref: '#/components/schemas/TransactionType'
          example: expense
        - name: categoryId
          in: query
          description: Category id filter.
          schema:
            type: integer
            minimum: 1
          example: 1
        - name: accountId
          in: query
          description: Account id filter.
          schema:
            type: integer
            minimum: 1
          example: 1
        - name: q
          in: query
          description: Keyword filter matched against transaction notes and tags. Maximum 100 characters.
          schema:
            type: string
            maxLength: 100
          example: lunch
      responses:
        '200':
          description: Transaction list
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
            Link:
              $ref: '#/components/headers/Link'
            X-Total-Count:
              $ref: '#/components/headers/TotalCount'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/TransactionListResponse'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
    post:
      tags: [transactions]
      operationId: postTransactions
      summary: Create a transaction
      description: Creates a ledger transaction and returns the saved record.
      requestBody:
        description: Transaction details to create.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/TransactionRequest'
      responses:
        '201':
          description: Created transaction
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Location:
              $ref: '#/components/headers/Location'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
  /transactions/{id}:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    put:
      tags: [transactions]
      operationId: putTransactionsById
      summary: Update a transaction
      description: Replaces an existing ledger transaction with the supplied transaction details.
      parameters:
        - $ref: '#/components/parameters/Id'
      requestBody:
        description: Complete transaction details to replace the existing transaction.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/TransactionRequest'
      responses:
        '200':
          description: Updated transaction
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Transaction'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
    delete:
      tags: [transactions]
      operationId: deleteTransactionsById
      summary: Delete a transaction
      description: Deletes the selected ledger transaction.
      parameters:
        - $ref: '#/components/parameters/Id'
      responses:
        '200':
          $ref: '#/components/responses/Ok'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
  /ai/parse:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    post:
      tags: [ai]
      operationId: postAiParse
      summary: Parse ledger text with AI
      description: Parses natural-language ledger text into structured transaction fields.
      requestBody:
        description: Natural-language ledger text to parse into transaction fields.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/AIParseRequest'
      responses:
        '200':
          description: Parsed ledger text
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AIParseResponse'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
  /reports/summary:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    get:
      tags: [reports]
      operationId: getReportsSummary
      summary: Get report summary
      description: Returns summary totals, category breakdowns, account balances, and trends for the selected range.
      parameters:
        - name: start
          in: query
          description: Inclusive start datetime. Accepts RFC3339, YYYY-MM-DD, YYYY-MM-DD HH:mm:ss, or YYYY/MM/DD.
          schema:
            type: string
            format: date-time
          example: '2026-06-01T00:00:00+08:00'
        - name: end
          in: query
          description: Inclusive end datetime. Date-only values cover the entire day. Accepts RFC3339, YYYY-MM-DD, YYYY-MM-DD HH:mm:ss, or YYYY/MM/DD.
          schema:
            type: string
            format: date-time
          example: '2026-06-30'
        - name: categoryId
          in: query
          description: Category id filter.
          schema:
            type: integer
            minimum: 1
          example: 1
        - name: accountId
          in: query
          description: Account id filter.
          schema:
            type: integer
            minimum: 1
          example: 1
        - name: trend
          in: query
          description: Trend aggregation granularity. Defaults to month when omitted.
          schema:
            type: string
            enum: [day, week, month]
            default: month
          example: month
      responses:
        '200':
          description: Report summary
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Summary'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
  /io/import/preview:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    post:
      tags: [dataio]
      operationId: postIoImportPreview
      summary: Preview an uploaded import file
      description: Parses an uploaded CSV or XLSX file and returns the import preview without saving rows.
      requestBody:
        description: CSV or XLSX ledger file to parse without saving records.
        required: true
        content:
          multipart/form-data:
            description: 'Accepts `Content-Type: multipart/form-data; boundary=<non-empty-boundary>` with a required `file` field.'
            schema:
              $ref: '#/components/schemas/ImportFileRequest'
      responses:
        '200':
          description: Import preview
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportPreview'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
  /io/import:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    post:
      tags: [dataio]
      operationId: postIoImport
      summary: Import an uploaded file
      description: Parses an uploaded CSV or XLSX file and saves valid rows.
      requestBody:
        description: CSV or XLSX ledger file to parse and save records.
        required: true
        content:
          multipart/form-data:
            description: 'Accepts `Content-Type: multipart/form-data; boundary=<non-empty-boundary>` with a required `file` field.'
            schema:
              $ref: '#/components/schemas/ImportFileRequest'
      responses:
        '200':
          description: Import result
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportResult'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
  /io/import/jobs:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    get:
      tags: [dataio]
      operationId: getIoImportJobs
      summary: List import jobs
      description: Returns recent asynchronous import jobs.
      responses:
        '200':
          description: Import job history
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                type: array
                items:
                  $ref: '#/components/schemas/ImportJob'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
    post:
      tags: [dataio]
      operationId: postIoImportJobs
      summary: Queue an import job
      description: Queues an uploaded CSV or XLSX file for asynchronous import.
      requestBody:
        description: CSV or XLSX ledger file to import asynchronously.
        required: true
        content:
          multipart/form-data:
            description: 'Accepts `Content-Type: multipart/form-data; boundary=<non-empty-boundary>` with a required `file` field.'
            schema:
              $ref: '#/components/schemas/ImportFileRequest'
      responses:
        '202':
          description: Queued import job
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Location:
              $ref: '#/components/headers/Location'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportJob'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
  /io/import/jobs/{id}:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    get:
      tags: [dataio]
      operationId: getIoImportJobsById
      summary: Get import job status
      description: Returns the current status and result metadata for a queued import job.
      parameters:
        - $ref: '#/components/parameters/Id'
      responses:
        '200':
          description: Import job status
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportJob'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '404':
          $ref: '#/components/responses/NotFound'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
  /io/import/text/preview:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    post:
      tags: [dataio]
      operationId: postIoImportTextPreview
      summary: Preview pasted import text
      description: Parses pasted ledger text and returns the import preview without saving rows.
      requestBody:
        description: Raw ledger text to parse without saving records.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/ImportTextRequest'
      responses:
        '200':
          description: Text import preview
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportPreview'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
  /io/import/text:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    post:
      tags: [dataio]
      operationId: postIoImportText
      summary: Import pasted text
      description: Parses pasted ledger text and saves valid rows.
      requestBody:
        description: Raw ledger text to parse and save records.
        required: true
        content:
          application/json:
            description: 'Accepts `Content-Type: application/json`, optional media type parameters such as `charset=utf-8`, and `application/*+json` structured JSON media types.'
            schema:
              $ref: '#/components/schemas/ImportTextRequest'
      responses:
        '200':
          description: Text import result
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ImportResult'
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '413':
          $ref: '#/components/responses/PayloadTooLarge'
        '415':
          $ref: '#/components/responses/UnsupportedMediaType'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
  /io/export:
    parameters:
      - $ref: '#/components/parameters/RequestID'
    get:
      tags: [dataio]
      operationId: getIoExport
      summary: Export transactions
      description: Exports transactions as CSV or XLSX for the selected date range.
      parameters:
        - name: format
          in: query
          description: Export file format. Values are trimmed and case-insensitive; defaults to csv when omitted.
          schema:
            type: string
            enum: [csv, xlsx]
            default: csv
          example: csv
        - name: start
          in: query
          description: Inclusive start datetime. Accepts RFC3339, YYYY-MM-DD, YYYY-MM-DD HH:mm:ss, or YYYY/MM/DD.
          schema:
            type: string
            format: date-time
          example: '2026-06-01T00:00:00+08:00'
        - name: end
          in: query
          description: Inclusive end datetime. Date-only values cover the entire day. Accepts RFC3339, YYYY-MM-DD, YYYY-MM-DD HH:mm:ss, or YYYY/MM/DD.
          schema:
            type: string
            format: date-time
          example: '2026-06-30'
      responses:
        '200':
          description: Exported ledger file
          headers:
            Cache-Control:
              $ref: '#/components/headers/CacheControl'
            Pragma:
              $ref: '#/components/headers/Pragma'
            Expires:
              $ref: '#/components/headers/Expires'
            Vary:
              $ref: '#/components/headers/Vary'
            X-Request-ID:
              $ref: '#/components/headers/RequestID'
            Content-Disposition:
              $ref: '#/components/headers/ContentDisposition'
          content:
            text/csv:
              schema:
                type: string
                format: binary
            application/vnd.openxmlformats-officedocument.spreadsheetml.sheet:
              schema:
                type: string
                format: binary
        '400':
          $ref: '#/components/responses/InvalidRequest'
        '401':
          $ref: '#/components/responses/Unauthorized'
        '406':
          $ref: '#/components/responses/NotAcceptable'
        '405':
          $ref: '#/components/responses/MethodNotAllowed'
        '500':
          $ref: '#/components/responses/InternalError'
        '504':
          $ref: '#/components/responses/GatewayTimeout'
        '499':
          $ref: '#/components/responses/ClientClosedRequest'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: >
        Send the access token as `Authorization: Bearer <JWT>`. At runtime, repeated `Authorization` headers are rejected, and issued JWTs include an `nbf` not-before claim.
  parameters:
    Id:
      name: id
      in: path
      required: true
      description: Positive resource identifier.
      schema:
        type: integer
        minimum: 1
      example: 1
    RequestID:
      name: X-Request-ID
      in: header
      required: false
      description: Optional request correlation id. The API trims surrounding whitespace and returns the normalized value when valid; missing, duplicate, or invalid values are replaced with a server-generated id that remains unique even if the system entropy source is unavailable.
      schema:
        type: string
        minLength: 1
        maxLength: 128
        pattern: '^[!-~]+$'
      example: client-request-123
  headers:
    RequestID:
      description: Request correlation id for tracing logs and API calls. Generated ids remain unique even if the system entropy source is unavailable.
      schema:
        type: string
        minLength: 1
        maxLength: 128
        pattern: '^[!-~]+$'
      example: client-request-123
    Link:
      description: RFC 8288 pagination links for first, previous, next, and last pages when available; sensitive query parameters are omitted from generated links.
      schema:
        type: string
      example: </api/v1/transactions?page=2&pageSize=20>; rel="next", </api/v1/transactions?page=5&pageSize=20>; rel="last"
    TotalCount:
      description: Total number of records matching the query before pagination.
      schema:
        type: integer
        minimum: 0
      example: 95
    ContentDisposition:
      description: Attachment disposition for downloaded files. Includes both filename and RFC 5987 filename* parameters.
      schema:
        type: string
      example: attachment; filename="transactions.csv"; filename*=UTF-8''transactions.csv
    Location:
      description: Relative URL of the created or queued API resource.
      schema:
        type: string
      example: /api/v1/io/import/jobs/1
    CacheControl:
      description: Cache policy for API responses containing user or authentication data.
      schema:
        type: string
        enum: [no-store]
      example: no-store
    Pragma:
      description: Legacy HTTP/1.0 cache policy retained for intermediary compatibility.
      schema:
        type: string
        enum: [no-cache]
      example: no-cache
    Expires:
      description: Legacy cache expiry value retained for intermediary compatibility.
      schema:
        type: string
        enum: ['0']
      example: '0'
    Vary:
      description: Response cache keys vary by negotiated response and CORS request headers.
      schema:
        type: string
      example: Accept, Origin, Access-Control-Request-Method, Access-Control-Request-Headers
    Allow:
      description: Comma-separated HTTP methods supported by the target route.
      schema:
        type: string
      example: GET, POST
    ClearSiteData:
      description: Browser-side data cleared after logout for this origin.
      schema:
        type: string
      example: '"cache", "cookies", "storage"'
    WWWAuthenticate:
      description: Authentication challenge for protected resources. Challenges use realm="haohao-accounting-api"; invalid bearer tokens also include error="invalid_token" and error_description per RFC 6750.
      schema:
        type: string
      example: Bearer realm="haohao-accounting-api", error="invalid_token", error_description="The access token is missing, expired, revoked, or invalid"
    RetryAfter:
      description: Remaining wait time before retrying, as either HTTP-date or non-negative integer delay-seconds.
      schema:
        type: string
      example: '60'
    RateLimitLimit:
      description: Maximum number of failed login attempts permitted in the active rate-limit window.
      schema:
        type: integer
        minimum: 1
      example: 5
    RateLimitRemaining:
      description: Remaining failed login attempts before the client is blocked.
      schema:
        type: integer
        minimum: 0
      example: 0
    RateLimitReset:
      description: Delay in seconds until the active rate-limit window resets.
      schema:
        type: integer
        minimum: 0
      example: 60
  responses:
    BadRequest:
      description: Bad request or validation error.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: validation failed
            code: bad_request
            status: 400
            requestId: api-20260620-example
    InvalidRequest:
      description: Request parameters or body failed parsing or binding.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: invalid request body
            code: invalid_request
            status: 400
            requestId: api-20260620-example
    Unauthorized:
      description: Missing, invalid, or expired authentication token.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
        WWW-Authenticate:
          $ref: '#/components/headers/WWWAuthenticate'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: invalid Authorization format
            code: unauthorized
            status: 401
            requestId: api-20260620-example
    Forbidden:
      description: Authenticated user cannot perform this action.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: forbidden
            code: forbidden
            status: 403
            requestId: api-20260620-example
    NotFound:
      description: Requested resource was not found.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: route not found
            code: not_found
            status: 404
            requestId: api-20260620-example
    MethodNotAllowed:
      description: HTTP method is not supported for this route.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
        Allow:
          $ref: '#/components/headers/Allow'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: method not allowed
            code: method_not_allowed
            status: 405
            requestId: api-20260620-example
    RateLimited:
      description: Too many requests.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
        Retry-After:
          $ref: '#/components/headers/RetryAfter'
        RateLimit-Limit:
          $ref: '#/components/headers/RateLimitLimit'
        RateLimit-Remaining:
          $ref: '#/components/headers/RateLimitRemaining'
        RateLimit-Reset:
          $ref: '#/components/headers/RateLimitReset'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: 登录失败次数过多，请稍后再试
            code: rate_limited
            status: 429
            requestId: api-20260620-example
    PayloadTooLarge:
      description: Request body is too large.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: request body too large
            code: payload_too_large
            status: 413
            requestId: api-20260620-example
    UnsupportedMediaType:
      description: Request Content-Type is not supported by this operation.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: 'unsupported media type: expected application/json'
            code: unsupported_media_type
            status: 415
            requestId: api-20260620-example
    NotAcceptable:
      description: Requested response media type is not available for this operation.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        Vary:
          $ref: '#/components/headers/Vary'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: 'not acceptable: expected application/json'
            code: not_acceptable
            status: 406
            requestId: api-20260620-example
    InternalError:
      description: Internal server error.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: internal server error
            code: internal_error
            status: 500
            requestId: api-20260620-example
    GatewayTimeout:
      description: Request processing exceeded the server timeout budget.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: request timed out
            code: request_timeout
            status: 504
            requestId: api-20260620-example
    ClientClosedRequest:
      description: Client canceled the request before the server finished processing.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: client closed request
            code: client_closed_request
            status: 499
            requestId: api-20260620-example
    Error:
      description: Error response. Prefer a status-specific response component when possible.
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/ErrorResponse'
          example:
            error: request failed
            code: internal_error
            status: 500
            requestId: api-20260620-example
    Ok:
      description: OK response
      headers:
        Cache-Control:
          $ref: '#/components/headers/CacheControl'
        Pragma:
          $ref: '#/components/headers/Pragma'
        Expires:
          $ref: '#/components/headers/Expires'
        Vary:
          $ref: '#/components/headers/Vary'
        X-Request-ID:
          $ref: '#/components/headers/RequestID'
      content:
        application/json:
          schema:
            $ref: '#/components/schemas/OkResponse'
  schemas:
    TransactionType:
      type: string
      enum: [income, expense]
    CurrentUser:
      type: object
      additionalProperties: false
      required: [id, name, username, phone, email, wechatId]
      properties:
        id:
          type: integer
          readOnly: true
        name:
          type: string
        username:
          type: string
        phone:
          type: string
        email:
          type: string
        wechatId:
          type: string
    LoginRequest:
      type: object
      additionalProperties: false
      required: [username, password]
      example:
        username: admin
        password: example-password
      properties:
        username:
          type: string
          minLength: 1
        password:
          type: string
          minLength: 1
          format: password
          writeOnly: true
    LoginResponse:
      type: object
      additionalProperties: false
      required: [token, user]
      example:
        token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.example.signature
        user:
          id: 1
          name: 好好用户
          username: admin
          phone: ''
          email: ''
          wechatId: ''
      properties:
        token:
          type: string
          readOnly: true
        user:
          $ref: '#/components/schemas/CurrentUser'
    Account:
      type: object
      additionalProperties: false
      required: [id, userId, name, type, balance, createdAt, updatedAt]
      properties:
        id:
          type: integer
          readOnly: true
        userId:
          type: integer
          readOnly: true
        name:
          type: string
        type:
          type: string
        balance:
          type: number
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    AccountRequest:
      type: object
      additionalProperties: false
      required: [name, type]
      example:
        name: 招商银行卡
        type: debit
        balance: 1200.5
      properties:
        name:
          type: string
          minLength: 1
        type:
          type: string
          minLength: 1
        balance:
          type: number
          minimum: 0
          multipleOf: 0.01
    Budget:
      type: object
      additionalProperties: false
      required: [id, userId, month, categoryId, amount, createdAt, updatedAt]
      properties:
        id:
          type: integer
          readOnly: true
        userId:
          type: integer
          readOnly: true
        month:
          type: string
        categoryId:
          type: integer
        amount:
          type: number
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    BudgetRequest:
      type: object
      additionalProperties: false
      required: [month, categoryId, amount]
      properties:
        month:
          type: string
          pattern: '^\d{4}-\d{2}$'
        categoryId:
          type: integer
          minimum: 1
        amount:
          type: number
          exclusiveMinimum: 0
          multipleOf: 0.01
    Category:
      type: object
      additionalProperties: false
      required: [id, name, type, isSystem, createdAt, updatedAt]
      properties:
        id:
          type: integer
          readOnly: true
        userId:
          type: integer
          readOnly: true
        name:
          type: string
        type:
          $ref: '#/components/schemas/TransactionType'
        isSystem:
          type: boolean
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    CategoryRequest:
      type: object
      additionalProperties: false
      required: [name, type]
      properties:
        name:
          type: string
          minLength: 1
        type:
          $ref: '#/components/schemas/TransactionType'
    Transaction:
      type: object
      additionalProperties: false
      required: [id, userId, type, amount, categoryId, accountId, note, tags, source, occurredAt, category, account, createdAt, updatedAt]
      properties:
        id:
          type: integer
          readOnly: true
        userId:
          type: integer
          readOnly: true
        type:
          $ref: '#/components/schemas/TransactionType'
        amount:
          type: number
        categoryId:
          type: integer
        accountId:
          type: integer
        note:
          type: string
        tags:
          type: string
        source:
          type: string
        occurredAt:
          type: string
          format: date-time
        category:
          $ref: '#/components/schemas/Category'
        account:
          $ref: '#/components/schemas/Account'
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    TransactionRequest:
      type: object
      additionalProperties: false
      required: [type, amount, categoryId, accountId, note]
      example:
        type: expense
        amount: 35.8
        categoryId: 2
        accountId: 1
        note: 午餐
        tags:
          - 工作日
        occurredAt: '2026-06-20T12:30:00+08:00'
        source: manual
      properties:
        type:
          $ref: '#/components/schemas/TransactionType'
        amount:
          type: number
          exclusiveMinimum: 0
          multipleOf: 0.01
        categoryId:
          type: integer
          minimum: 1
        accountId:
          type: integer
          minimum: 1
        note:
          type: string
          minLength: 1
        tags:
          type: array
          items:
            type: string
        occurredAt:
          type: string
          format: date-time
        source:
          type: string
    TransactionListResponse:
      type: object
      additionalProperties: false
      required: [items, pagination]
      properties:
        items:
          type: array
          items:
            $ref: '#/components/schemas/Transaction'
        pagination:
          $ref: '#/components/schemas/Pagination'
    Pagination:
      type: object
      additionalProperties: false
      required: [page, pageSize, total]
      properties:
        page:
          type: integer
          minimum: 1
        pageSize:
          type: integer
          minimum: 1
          maximum: 200
        total:
          type: integer
          minimum: 0
    AIParseRequest:
      type: object
      additionalProperties: false
      required: [text]
      properties:
        text:
          type: string
          minLength: 1
    AIParseResult:
      type: object
      additionalProperties: false
      required: [type, amount, category, account, note, occurredAt, confidence]
      properties:
        type:
          $ref: '#/components/schemas/TransactionType'
        amount:
          type: number
        category:
          type: string
        account:
          type: string
        note:
          type: string
        occurredAt:
          type: string
          format: date-time
        confidence:
          type: number
          minimum: 0
          maximum: 1
    AIParseResponse:
      type: object
      additionalProperties: false
      required: [requiresConfirmation, cached, result]
      properties:
        requiresConfirmation:
          type: boolean
        cached:
          type: boolean
        result:
          $ref: '#/components/schemas/AIParseResult'
    CategoryStat:
      type: object
      additionalProperties: false
      required: [categoryId, category, amount]
      example:
        categoryId: 1
        category: 餐饮
        amount: 250
      properties:
        categoryId:
          type: integer
          minimum: 1
        category:
          type: string
        amount:
          type: number
          minimum: 0
          multipleOf: 0.01
    AccountStat:
      type: object
      additionalProperties: false
      required: [accountId, account, amount]
      example:
        accountId: 1
        account: 现金
        amount: 250
      properties:
        accountId:
          type: integer
          minimum: 1
        account:
          type: string
        amount:
          type: number
          minimum: 0
          multipleOf: 0.01
    MonthTrend:
      type: object
      additionalProperties: false
      required: [month, income, expense]
      example:
        month: '2026-06'
        income: 3000
        expense: 250
      properties:
        month:
          type: string
          pattern: '^\d{4}-\d{2}$'
        income:
          type: number
          minimum: 0
          multipleOf: 0.01
        expense:
          type: number
          minimum: 0
          multipleOf: 0.01
    TrendPoint:
      type: object
      additionalProperties: false
      required: [period, income, expense]
      example:
        period: '2026-06-01'
        income: 3000
        expense: 100
      properties:
        period:
          type: string
        income:
          type: number
          minimum: 0
          multipleOf: 0.01
        expense:
          type: number
          minimum: 0
          multipleOf: 0.01
    CategoryTrendPoint:
      type: object
      additionalProperties: false
      required: [period, categoryId, category, amount]
      example:
        period: '2026-06-01'
        categoryId: 1
        category: 餐饮
        amount: 100
      properties:
        period:
          type: string
        categoryId:
          type: integer
          minimum: 1
        category:
          type: string
        amount:
          type: number
          minimum: 0
          multipleOf: 0.01
    AccountBalancePoint:
      type: object
      additionalProperties: false
      required: [period, accountId, account, net, balance]
      example:
        period: '2026-06-02'
        accountId: 1
        account: 现金
        net: -150
        balance: 2750
      properties:
        period:
          type: string
        accountId:
          type: integer
          minimum: 1
        account:
          type: string
        net:
          type: number
          multipleOf: 0.01
        balance:
          type: number
          multipleOf: 0.01
    BudgetExecution:
      type: object
      additionalProperties: false
      required: [budgetId, month, categoryId, category, budget, expense, remaining, usageRate]
      example:
        budgetId: 1
        month: '2026-06'
        categoryId: 1
        category: 餐饮
        budget: 500
        expense: 250
        remaining: 250
        usageRate: 0.5
      properties:
        budgetId:
          type: integer
          minimum: 1
        month:
          type: string
          pattern: '^\d{4}-\d{2}$'
        categoryId:
          type: integer
          minimum: 0
        category:
          type: string
        budget:
          type: number
          minimum: 0
          multipleOf: 0.01
        expense:
          type: number
          minimum: 0
          multipleOf: 0.01
        remaining:
          type: number
          multipleOf: 0.01
        usageRate:
          type: number
          minimum: 0
    SummaryTableRow:
      type: object
      additionalProperties: false
      required: [period, income, expense, balance, txCount]
      example:
        period: '2026-06-02'
        income: 0
        expense: 150
        balance: -150
        txCount: 1
      properties:
        period:
          type: string
        income:
          type: number
          minimum: 0
          multipleOf: 0.01
        expense:
          type: number
          minimum: 0
          multipleOf: 0.01
        balance:
          type: number
          multipleOf: 0.01
        txCount:
          type: integer
          minimum: 0
    PeriodTotals:
      type: object
      additionalProperties: false
      required: [income, expense]
      example:
        income: 3000
        expense: 250
      properties:
        income:
          type: number
          minimum: 0
          multipleOf: 0.01
        expense:
          type: number
          minimum: 0
          multipleOf: 0.01
    PeriodCompare:
      type: object
      additionalProperties: false
      required: [current, previous]
      example:
        current:
          income: 3000
          expense: 250
        previous:
          income: 0
          expense: 0
      properties:
        current:
          $ref: '#/components/schemas/PeriodTotals'
        previous:
          $ref: '#/components/schemas/PeriodTotals'
    Summary:
      type: object
      additionalProperties: false
      required: [start, end, income, expense, balance, byCategory, byAccount, monthlyTrend, trendGranularity, trend, categoryTrend, accountBalanceTrend, budgetExecution, dailySummaries, monthlySummaries, periodCompare]
      example:
        start: '2026-06-01T00:00:00Z'
        end: '2026-06-30T23:59:59Z'
        income: 3000
        expense: 250
        balance: 2750
        byCategory:
          - categoryId: 1
            category: 餐饮
            amount: 250
        byAccount:
          - accountId: 1
            account: 现金
            amount: 250
        monthlyTrend:
          - month: '2026-06'
            income: 3000
            expense: 250
        trendGranularity: day
        trend:
          - period: '2026-06-01'
            income: 3000
            expense: 100
          - period: '2026-06-02'
            income: 0
            expense: 150
        categoryTrend:
          - period: '2026-06-01'
            categoryId: 1
            category: 餐饮
            amount: 100
        accountBalanceTrend:
          - period: '2026-06-02'
            accountId: 1
            account: 现金
            net: -150
            balance: 2750
        budgetExecution:
          - budgetId: 1
            month: '2026-06'
            categoryId: 1
            category: 餐饮
            budget: 500
            expense: 250
            remaining: 250
            usageRate: 0.5
        dailySummaries:
          - period: '2026-06-02'
            income: 0
            expense: 150
            balance: -150
            txCount: 1
        monthlySummaries:
          - period: '2026-06'
            income: 3000
            expense: 250
            balance: 2750
            txCount: 3
        periodCompare:
          current:
            income: 3000
            expense: 250
          previous:
            income: 0
            expense: 0
      properties:
        start:
          type: string
          format: date-time
        end:
          type: string
          format: date-time
        income:
          type: number
          minimum: 0
          multipleOf: 0.01
        expense:
          type: number
          minimum: 0
          multipleOf: 0.01
        balance:
          type: number
          multipleOf: 0.01
        byCategory:
          type: array
          items:
            $ref: '#/components/schemas/CategoryStat'
        byAccount:
          type: array
          items:
            $ref: '#/components/schemas/AccountStat'
        monthlyTrend:
          type: array
          items:
            $ref: '#/components/schemas/MonthTrend'
        trendGranularity:
          type: string
          enum: [day, week, month]
        trend:
          type: array
          items:
            $ref: '#/components/schemas/TrendPoint'
        categoryTrend:
          type: array
          items:
            $ref: '#/components/schemas/CategoryTrendPoint'
        accountBalanceTrend:
          type: array
          items:
            $ref: '#/components/schemas/AccountBalancePoint'
        budgetExecution:
          type: array
          items:
            $ref: '#/components/schemas/BudgetExecution'
        dailySummaries:
          type: array
          items:
            $ref: '#/components/schemas/SummaryTableRow'
        monthlySummaries:
          type: array
          items:
            $ref: '#/components/schemas/SummaryTableRow'
        periodCompare:
          $ref: '#/components/schemas/PeriodCompare'
    ImportFileRequest:
      type: object
      additionalProperties: false
      description: CSV/XLSX import file. The first row must start with occurred_at,type,amount,category,account,note,tags; a UTF-8 BOM on the first header is accepted and additional columns are ignored.
      required: [file]
      properties:
        file:
          type: string
          format: binary
          description: CSV or XLSX file. The first row must start with occurred_at,type,amount,category,account,note,tags; a UTF-8 BOM on the first header is accepted.
        skipDuplicates:
          type: boolean
          description: skipDuplicates defaults to true when omitted; invalid values return invalid_request.
          default: true
    ImportTextRequest:
      type: object
      additionalProperties: false
      required: [content]
      properties:
        filename:
          type: string
        content:
          type: string
          description: CSV text. The first row must start with occurred_at,type,amount,category,account,note,tags; a UTF-8 BOM on the first header is accepted.
          minLength: 1
          maxLength: 5242880
        skipDuplicates:
          type: boolean
          description: skipDuplicates defaults to true when omitted.
          default: true
    ImportPreviewRow:
      type: object
      additionalProperties: false
      required: [line, occurredAt, type, amount, category, account, note, tags, valid, duplicate]
      example:
        line: 2
        occurredAt: '2026-06-01T12:30:00+08:00'
        type: expense
        amount: 35.5
        category: 餐饮
        account: 现金
        note: 午饭
        tags: 工作日
        valid: true
        duplicate: false
      properties:
        line:
          type: integer
          minimum: 1
        occurredAt:
          type: string
        type:
          type: string
          description: Valid rows use income or expense; failed rows may return an empty string before parsing reaches the type field.
          enum: [income, expense, '']
        amount:
          type: number
          minimum: 0
          multipleOf: 0.01
        category:
          type: string
        account:
          type: string
        note:
          type: string
        tags:
          type: string
        valid:
          type: boolean
        duplicate:
          type: boolean
        error:
          type: string
        duplicateReason:
          type: string
    ImportPreview:
      type: object
      additionalProperties: false
      required: [filename, size, totalRows, validRows, failedRows, duplicateRows, maxRows, maxFileBytes, truncated, rows]
      example:
        filename: preview.csv
        size: 154
        totalRows: 2
        validRows: 1
        failedRows: 1
        duplicateRows: 0
        maxRows: 5000
        maxFileBytes: 5242880
        truncated: false
        rows:
          - line: 2
            occurredAt: '2026-06-01T12:30:00+08:00'
            type: expense
            amount: 35.5
            category: 餐饮
            account: 现金
            note: 午饭
            tags: 工作日
            valid: true
            duplicate: false
          - line: 3
            occurredAt: ''
            type: ''
            amount: 0
            category: ''
            account: ''
            note: ''
            tags: ''
            valid: false
            duplicate: false
            error: invalid occurred_at
      properties:
        filename:
          type: string
        size:
          type: integer
          minimum: 0
        totalRows:
          type: integer
          minimum: 0
          description: Non-empty data rows excluding the header and blank rows.
        validRows:
          type: integer
          minimum: 0
        failedRows:
          type: integer
          minimum: 0
        duplicateRows:
          type: integer
          minimum: 0
        maxRows:
          type: integer
          minimum: 0
        maxFileBytes:
          type: integer
          minimum: 0
        truncated:
          type: boolean
        rows:
          type: array
          maxItems: 20
          items:
            $ref: '#/components/schemas/ImportPreviewRow'
    ImportResult:
      type: object
      additionalProperties: false
      required: [total, success, failed, skipped, errors]
      example:
        total: 2
        success: 1
        failed: 1
        skipped: 0
        errors:
          - 'line 3: invalid occurred_at'
      properties:
        total:
          type: integer
          minimum: 0
        success:
          type: integer
          minimum: 0
        failed:
          type: integer
          minimum: 0
        skipped:
          type: integer
          minimum: 0
        errors:
          type: array
          items:
            type: string
    ImportJob:
      type: object
      additionalProperties: false
      required: [id, filename, status, total, success, failed, skipped, errors, createdAt, updatedAt]
      example:
        id: 1
        filename: import.csv
        status: completed
        total: 2
        success: 1
        failed: 1
        skipped: 0
        errors:
          - 'line 3: invalid occurred_at'
        createdAt: '2026-06-01T12:30:00+08:00'
        updatedAt: '2026-06-01T12:30:05+08:00'
      properties:
        id:
          type: integer
          minimum: 1
          readOnly: true
        filename:
          type: string
        status:
          type: string
          enum: [queued, running, completed, failed]
        total:
          type: integer
          minimum: 0
        success:
          type: integer
          minimum: 0
        failed:
          type: integer
          minimum: 0
        skipped:
          type: integer
          minimum: 0
        errors:
          type: array
          items:
            type: string
        createdAt:
          type: string
          format: date-time
          readOnly: true
        updatedAt:
          type: string
          format: date-time
          readOnly: true
    ErrorResponse:
      type: object
      additionalProperties: false
      required: [error, code, status, requestId]
      example:
        error: invalid Authorization format
        code: unauthorized
        status: 401
        requestId: api-20260620-example
      properties:
        error:
          type: string
        code:
          type: string
          enum: [bad_request, invalid_request, unauthorized, forbidden, not_found, method_not_allowed, rate_limited, payload_too_large, unsupported_media_type, not_acceptable, request_timeout, client_closed_request, internal_error]
        status:
          type: integer
          minimum: 400
          maximum: 599
        requestId:
          type: string
    OkResponse:
      type: object
      additionalProperties: false
      required: [ok]
      properties:
        ok:
          type: boolean
