# Backend Internal Packages

Internal backend code is split into composition, business modules, shared helpers, models, and infrastructure adapters.

New business features should usually start under `internal/modules/<feature>`.
