# Query Utilities

Small HTTP query parsing helpers shared by handlers.

Handlers should use these helpers for basic string-to-number conversion, then pass typed values into their services. Business validation still belongs in each module service.
