# Backend Services

Small standalone services that are shared across modules.

The current parser is used by the `ai` module. If a service grows feature-specific state or dependencies, move it into that module.
