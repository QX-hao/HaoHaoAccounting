# iOS simulator / Android emulator can use 127.0.0.1 if backend runs on same host
# Public client variable. Use an absolute http(s) URL without a trailing slash;
# this value is bundled into the app, so do not put secrets here.
EXPO_PUBLIC_API_BASE=http://127.0.0.1:8080/api/v1
