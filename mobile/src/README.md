# Mobile Source

React Native/Expo source code.

## Structure

- `features`: screen-level business modules.
- `navigation`: lightweight tab state and tab bar.
- `shared`: API client, types, and utilities.
- `theme`: shared styles.

`App.tsx` should remain the root coordinator, not a place for full screen UI or styles.
